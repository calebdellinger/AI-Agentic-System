# Example Regional Concrete LLC (sample tenant)

**Not a real company.** This folder demonstrates how **field + back-office + reference** docs plug into the shared knowledge tree.

To adopt another business: copy `../_template` (if present) or this structure, change `manifest.json`, and set `COMPANY_KNOWLEDGE_SLUG` to the new directory name.
